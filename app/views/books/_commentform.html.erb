<% if @book_comment.errors.any? %>
  <div id="error_explanation">
    <h3><%= pluralize(@book_comment.errors.count, "error") %> prohibited this obj from being saved:</h3>
    <ul>
      <% @book_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
<%= form_with  url: book_book_comments_path(book.id), remote: true do |f| %>
    <div class="form-group">
        <%= f.text_area :comment, class: 'form-control book_body' %>
    </div>
    <div class="form-group">
        <%= f.submit "送信", class: 'btn btn-light btn-outline-dark' %>
    </div>
<% end %>